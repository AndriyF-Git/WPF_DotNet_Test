<Page x:Class="WPF_DotNet_Test.Views.SearchPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Background="{DynamicResource BackgroundBrush}"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SearchPage">

    <Grid>
        <DockPanel Margin="24,20,24,0">

            <!-- Header -->
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,16">
                <TextBlock Text="Search"
                           FontSize="24" FontWeight="SemiBold"/>
                <TextBlock Text="Find a coin by name or symbol"
                           FontSize="13"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Search box -->
            <DockPanel DockPanel.Dock="Top" Margin="0,0,0,16">
                <Button DockPanel.Dock="Right"
                        Content="Search"
                        Style="{DynamicResource AccentButtonStyle}"
                        Command="{Binding SearchCommand}"
                        Margin="8,0,0,0"/>
                <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
            </DockPanel>

            <!-- Results -->
            <ScrollViewer DockPanel.Dock="Top" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Results}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Style="{DynamicResource CoinRowButtonStyle}"
                                    Command="{Binding DataContext.SelectResultCommand,
                                              RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="50"/>
                                        <ColumnDefinition Width="44"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{Binding MarketCapRank}"
                                               FontSize="13"
                                               Foreground="{DynamicResource SecondaryTextBrush}"
                                               VerticalAlignment="Center"/>

                                    <Image Grid.Column="1"
                                           Source="{Binding Thumb}"
                                           Width="28" Height="28"
                                           RenderOptions.BitmapScalingMode="HighQuality"
                                           VerticalAlignment="Center"/>

                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="4,0,0,0">
                                        <TextBlock Text="{Binding Name}"
                                                   FontSize="14" FontWeight="Medium"/>
                                        <TextBlock Text="{Binding Symbol}"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource SecondaryTextBrush}"/>
                                    </StackPanel>
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>

        <!-- ── Loading Overlay ── -->
        <Grid Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
              Background="#AA1E1E2E">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid Width="50" Height="50" Margin="0,0,0,12">
                    <Ellipse Width="44" Height="44"
                             Stroke="#33FFFFFF"
                             StrokeThickness="4"/>
                    <Ellipse Width="44" Height="44"
                             Stroke="{DynamicResource AccentBrush}"
                             StrokeThickness="4"
                             StrokeDashArray="20 50"
                             StrokeDashCap="Round">
                        <Ellipse.RenderTransform>
                            <RotateTransform CenterX="22" CenterY="22"/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation
                                            Storyboard.TargetProperty="RenderTransform.(RotateTransform.Angle)"
                                            From="0" To="360" Duration="0:0:1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                </Grid>
                <TextBlock Text="Searching…"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           FontSize="14"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- ── Error Banner ── -->
        <Border Visibility="{Binding HasStatusMessage, Converter={StaticResource BooleanToVisibilityConverter}}"
                Background="{DynamicResource CardBrush}"
                BorderBrush="{DynamicResource NegativeBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16,10"
                Margin="24"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Stretch">
            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="{DynamicResource NegativeBrush}"
                       TextWrapping="Wrap"/>
        </Border>
    </Grid>
</Page>
